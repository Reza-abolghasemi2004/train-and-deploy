service:
    nexus:
      images: sonatype/nexus3
      container_name: nexus
      hostname: nexus
      
      restart: unless-stopped

      volumes:
        - "nexus/data:/sonatype-work"
      ports:
        - "8081:8081"

volumes:
  nexus-data: {}
